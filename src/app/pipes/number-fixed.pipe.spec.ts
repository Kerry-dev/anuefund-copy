import { NumberFixedPipe } from './number-fixed.pipe';

describe('NumberFixedPipe', () => {
  it('create an instance', () => {
    const pipe = new NumberFixedPipe();
    expect(pipe).toBeTruthy();
  });
});
